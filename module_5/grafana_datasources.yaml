# config file version
apiVersion: 1

# list of datasources to insert/update
# available in the database
datasources:
  - name: NewMonitorPostgreSQL
    type: postgres
    url: db:5432
    database: test_homework5
    user: postgres
    secureJsonData:
      password: 'example'
    jsonData:
      sslmode: 'disable'